# ---- Core API ----
fastapi==0.128.0
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.15.0
python-multipart==0.0.21

# ---- Database ----
SQLAlchemy==2.0.46
alembic==1.19.2
greenlet>=3.2.0
aiosqlite==0.22.1          # zero-install dev driver
asyncpg==0.31.0            # PostgreSQL (docker-compose / production)

# ---- Auth & security ----
python-jose[cryptography]==3.5.0
# passlib intentionally dropped: its bcrypt backend probe fails on bcrypt>=4.1
bcrypt==5.0.0

# ---- Task queue (production path) ----
celery==5.6.3
redis==8.1.0

# ---- Object storage (production path) ----
boto3>=1.35.0              # MinIO / S3 compatible

# ---- Computer vision & extraction ----
opencv-python-headless==5.0.0.93
numpy>=2.0
Pillow==12.1.0
imagehash==4.3.2
pyzbar==0.1.9              # needs libzbar; graceful fallback if absent

# ---- Geospatial ----
shapely==2.1.2

# ---- Vision LLM providers ----
anthropic>=0.40.0          # Claude Vision (primary)
google-genai>=2.0.0        # Gemini Vision (alternate)

# ---- Reporting ----
reportlab==4.4.9           # dev PDF engine
qrcode[pil]==8.2
# weasyprint==63.0         # production PDF engine (docker image only; needs GTK on Windows)

# ---- HTTP & utils ----
httpx>=0.27
python-dateutil>=2.9
